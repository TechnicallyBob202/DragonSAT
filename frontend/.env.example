NEXT_PUBLIC_API_URL=http://localhost:3001/api

# Backend URL for the API proxy (server-side only, no NEXT_PUBLIC_)
BACKEND_URL=http://localhost:3001/api

# Google OAuth (optional — leave blank to disable Google Sign-In button)
NEXT_PUBLIC_GOOGLE_CLIENT_ID=
