# DragonSAT Docker deployment environment
# Copy this file to .env and fill in your values.
# docker-compose reads .env automatically from this directory.

# ── Backend ───────────────────────────────────────────────────────────────────
NODE_ENV=production
PORT=3001
DATABASE_PATH=/app/data/dragonsat.db
OPENSAT_API_URL=https://pinesat.com/api/questions
FRONTEND_URL=http://localhost:3000

# Generate a strong random secret: openssl rand -hex 32
JWT_SECRET=change-me-to-a-long-random-secret

# ── Google OAuth (optional) ───────────────────────────────────────────────────
# Leave blank to disable Google Sign-In entirely.
# Get credentials at https://console.cloud.google.com/
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Must match GOOGLE_CLIENT_ID — baked into the Next.js bundle at build time.
NEXT_PUBLIC_GOOGLE_CLIENT_ID=
